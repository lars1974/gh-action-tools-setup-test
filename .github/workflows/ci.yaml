name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"

      - name: my-second-step
        uses: lars1974/gh-actions-tools-setup@v1

      - run: helm version
        name: Check Helm version

      - run: java -version
        name: Check Java version



